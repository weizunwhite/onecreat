# OneCreat 架构重构 Plan 00–12 交付报告

> 日期：2026-08-21  
> 基线：`main-v2@3feb3d7`  
> 栈顶:`docs/final-evidence-tables`(PR #32;整改栈 #15–#32,见 §2)  
> **本报告不宣称"完成"。** 按用户复核报告 §8 的 Definition of Done,还差**两项**,列在 §7:
> 没有 CI 证据(基础设施故障,需用户处理),以及 4 条 finding 未闭包(dsh 协议缺陷,需用户裁决)。
> 两项都是任务书自己列的**停止条件**,不是可以绕过去的东西。

---

## 1. Plan 00–12 状态表

| Plan | 交付 | 复核裁决 | 整改后 |
|---|---|---|---|
| 00 传输边界收口 + CI 基线 | ✅ | 基本通过，CI 缺 Frontend | Frontend 已进 CI（#21） |
| 01 显式 WorkspaceContext | ✅ | 部分完成:`.env` 仍进程全局 | 凭据解析按工作区隔离(#19);`.env` 不再 `os.Setenv`、子进程环境显式组装(#25),后期追加的插件补漏(#29) |
| 02 TabManager 单一真源 | ✅ | 基本通过 | — |
| 03 四级 Runtime Scope | ✅ | 部分完成：Turn 未接线 | Turn 已接线（#18） |
| 04 统一 RuntimeFactory | ✅ | 部分完成：dsh 仍装配 native 栈 | **延期**（dsh 已 fail-closed，降级为浪费） |
| 05 资源生命周期 | ✅ | 部分完成：双注册表竞态 | Factory 成唯一账本（#20） |
| 06 拆 desktop.App | ✅ | 基本通过 | — |
| 07 拆 control.Controller | ✅ | 部分完成:capability 未执行 | 改状态前校验(#15);HTTP 422/409(#28);UI 按能力禁用并说明原因(#30) |
| 08 Agent / toolpolicy 分离 | ✅ native | **跨引擎不成立**：dsh 不调用它 | dsh 在装配根 fail-closed（#15） |
| 09 Account / CredentialSource | ✅ | 部分完成：dsh token 与 `.env` | 子进程环境白名单（#15）；token 滚动**延期** |
| 10 Event Protocol V2 | ✅ | 部分完成:只断开、不恢复 | 权威快照 + 客户端对齐(#17);强制 overflow 端到端(#26);对齐重入修复(#31) |
| 11 Session 身份与元数据 | ✅ | **未通过**：Registry 丢更新 | 跨进程锁（#16）；dsh backend **延期** |
| 12 最小 TurnEngine + dsh | ✅ | **未通过**：dsh 集成不安全 | 接口成立；dsh **不允许启用**（#15） |

## 2. 每个 Plan 的 branch / PR / 验收 commit

| Plan | branch | PR | 代码 commit | CI |
|---|---|---|---|---|
| 00 | `refactor/runtime-context-tabs` | #2 | `842b6cf` | `32393263488` ✅ |
| 01 | `refactor/workspace-context` | #3 | `be639c2` | `32399141679` ✅ |
| 02 | `refactor/tab-manager` | #4 | `65fce1c` | `32401485722` ✅ |
| 03 | `refactor/runtime-scopes` | #5 | `972f954` | `32403239151` ✅ |
| 04 | `refactor/runtime-factory` | #6 | `81a4eb0` | `32404539929` ✅ |
| 05 | `refactor/resource-lifecycle` | #7 | `a464e2b` | `32407268249` ✅ |
| 06 | `refactor/desktop-app-services` | #8 | `1eea6ff` | `32410715551` ✅ |
| 07 | `refactor/controller-services` | #9 | `22c7a2c` | `32413724177` ✅ |
| 08 | `refactor/agent-policy-boundary` | #10 | `a264831` | `32415575294` ✅ |
| 09 | `refactor/account-credentials` | #11 | `32d599b` | `32417346801` ✅ |
| 10 | `refactor/event-protocol-v2` | #12 | `135aa81` | `32419068521` ✅ |
| 11 | `refactor/session-persistence` | #13 | `d578113` | ❌ 无 |
| 12 | `refactor/engine-dsh-adapter` | #14 | `1de0e80` | ❌ 无 |

### 复核整改分支

| Wave | Finding | branch | PR | head | CI |
|---|---|---|---|---|---|
| A | R01/R02/R03半/R04/R06半 | `fix/dsh-fail-closed-wave-a` | #15 | `1f4dfec` | ❌ |
| B | R09 | `fix/session-registry-cas` | #16 | `dc6cf3a` | ❌ |
| B | R07 | `fix/stream-resync` | #17 | `6a01e80` | ❌ |
| C | R12 | `fix/turn-scope-wiring` | #18 | `4dbc8d7` | ❌ |
| C | R10半 | `fix/workspace-env-overlay` | #19 | `fa331b8` | ❌ |
| C | R11 | `fix/factory-authoritative-entry` | #20 | `34d3d76` | ❌ |
| D | R13.2/R14 | `fix/ci-frontend-and-docs` | #21 | `f79863b` | ❌ |
| 补遗 | R08.1 | `fix/event-session-identity` | #22 | `cd223bd` | ❌ |
| 补遗 | R09 跨进程验收 | `fix/registry-cross-process-test` | #23 | `eceb7e7` | ❌ |
| 补遗 | dsh 集成验收 | `test/dsh-fake-sidecar` | #24 | `47104d7` | ❌ |
| C | R10 下半(C1) | `fix/architecture-review-2026-08-21` | #25 | `074a5bf` | ❌ |
| B | R07 端到端(B2) | `fix/stream-overflow-e2e` | #26 | `c48a85d` | ❌ |
| D | D2 + §22 阻断项 | `docs/review-remediation-log` | #27 | `e95182d` | ❌ |
| A | R02 第三句前半 | `fix/unsupported-http-status` | #28 | — | ❌ |
| C | C1 补漏 | `fix/late-added-plugin-env` | #29 | `3ee8fd3` | ❌ |
| A | R02 第三句后半 | `fix/ui-capability-gating` | #30 | `41970e4` | ❌ |
| B | 对齐重入修复 | `test/resync-buffer-exec` | #31 | — | ❌ |
| D | 集成复核 + 证据表 | `docs/final-evidence-tables` | #32 | — | ❌ |

全部 **Draft**,未 merge、未 auto-merge、未删分支。base 严格线性:
`refactor/engine-dsh-adapter` → #15 → … → #24 → #25 → … → #32。

## 3. 最终 CI matrix

**已取得(2026-08-21 10:36 UTC)。** 栈顶 `fix/windows-lock-contention`(19ccf2c,PR #37),
run **`32473178841`**:

| Job | 结果 | 时长 |
| --- | --- | --- |
| test (ubuntu-latest) | ✅ | 20s |
| test (macos-latest) | ✅ | 14s |
| **test (windows-latest)** | ✅ | **59s** |
| race | ✅ | 1m44s |
| desktop | ✅ | 1s |
| frontend | ✅ | 10s |

落在栈顶,因此覆盖 `main-v2...栈顶` 的累积 diff。

**CI 一恢复就抓到一个真回归**(整改自己引入的):索引锁在 Windows 上从来没生效过,
已修(总计划 §30)。这正是「本地全绿不能替代 CI 证据」的实证。

**#17–#36 仍红**:它们的 head 带修复前的 `lock.go`,要等修复经合并传播。不要逐个重跑。

---

以下是故障期间的记录,保留不删:

**曾经不存在。** 这是本报告最大的缺口，且不是代码问题。

- GitHub Actions 自 2026-08-20 21:29 UTC 起账号级故障：所有 run 的所有 job 在 2–6 秒内失败，
  零 step、无 annotation、日志 404；与 `ci.yml` 无关的 `Label PR version` 同样如此。
  截至 2026-08-21 04:45 UTC 持续 **7.3 小时**,二十余次 run 形态完全一致(§19.5)。
- 本会话没有 `actions: write`：`rerun-failed-jobs`、`rerun`、`workflows/ci.yml/dispatches`
  三个端点全部 403。
- **另一个刚发现的原因**：`ci.yml` 的 base 过滤原本只有 `[main-v2, "refactor/**"]`，
  而整改分支是 `fix/*` —— PR #16–#21 **只跑了 `label` 一个 job**，五腿矩阵从未触发。
  已在 #21 补上 `"fix/**"`,并在 §28 **彻底去掉了这个过滤器**(它第二次静默吞掉矩阵:
  base 为 `test/*` / `docs/*` 的 #25 / #28 / #32 从头到尾只跑了 `label`)。
  #21 那条补丁**被证明生效了**:PR #22 起的 run 都是 **6 个 job**
  (五腿矩阵 + 新增的 `frontend`)而不是 1 个 —— 例如 run `32441862643`、`32445758935`、
  `32448053980`。它们仍然全红,但红的是基础设施,不再是"根本没跑"。
  这两种失败必须分开看:后者看起来什么都没发生,比红 CI 更危险。

### 本地证据（每个分支都跑过）

| 项 | 命令 | 结果 |
|---|---|---|
| root 格式/静态 | `gofmt -l` / `go vet ./...` / `go build ./...` | 干净 |
| root 测试 | `REASONIX_RELEASE_CACHE_GUARD=1 go test ./...` | 全绿 |
| root 竞态 | `REASONIX_RELEASE_CACHE_GUARD=1 go test -race ./...` | 全绿 |
| 依赖 | `go mod tidy` | 无漂移 |
| desktop | gofmt / vet / build / `go test ./...` / `-tags web` / `-race ./` | 全绿 |
| frontend | `pnpm install --frozen-lockfile` / `tsc --noEmit` / `pnpm build` | 全绿 |
| Windows | `GOOS=windows go vet ./...` + 逐包 `go test -c` | 编译全过，**未运行** |
| macOS | `GOOS=darwin` 同上 | 编译全过，**未运行** |

**最后两行不算通过。** Plan 00 的 `842b6cf`（Windows 路径转义）与 Plan 01 的 `be639c2`
（`filepath.IsAbs` 语义）都是只在**运行期**才暴露的问题 —— 编译过不代表没有。

## 4. 最终架构摘要

```
        Desktop / CLI / Serve / ACP        ← 前端，只经 Controller
                    │
        ┌───────────▼───────────┐
        │  control.Controller   │  六个服务，各自持锁；Controller 自己无锁
        │  approval · session · │
        │  memory · mcp · ckpt  │
        │  · turnState          │
        └───────────┬───────────┘
    OneCreat 产品策略（memory / evidence / permission / billing / checkpoint）
                    │
        ┌───────────▼───────────┐
        │  engine.TurnEngine    │  一个方法：Start → TurnHandle{Cancel,Wait}
        └─────┬───────────┬─────┘
         native          dsh（已接线，装配根 fail-closed）
```

- **生命周期**：`runtime` 的 Process → Workspace → Session → Turn 四级全部接线；
  Workspace 按 root 引用计数共享，`boot.Factory` 是它的唯一账本。
- **配置**：`config.Env` 按工作区隔离；`engine` 是活配置项。
- **事件**：durable/ephemeral 分级 + V2 信封 + 权威快照与客户端对齐。
- **会话**：`internal/session` 是身份与元数据的唯一所有者，跨进程锁保护索引。

## 5. 已消除的架构反模式

| 反模式 | 消除方式 | 守卫 |
|---|---|---|
| workspace = 进程 cwd | 显式 `WorkspaceContext` | Plan 01 测试 |
| 活动 tab 的双状态真源 | `tabManager` 唯一真源 | `desktop/tabs_test.go` |
| 所有资源挂同一条 session 生命周期 | 四级 scope + Factory | `internal/runtime` 测试 |
| 平行装配链路（ACP 手抄 boot.Build） | 唯一装配入口 | `internal/acp` AST 守卫 |
| 巨型 `desktop.App`（3766 行） | 710 行 transport facade | `app_facade_test.go` |
| 巨型 `control.Controller`（1923 行） | 756 行 + 六服务 | `facade_test.go` |
| 工具调用策略散在 run loop | `internal/toolpolicy` | `policy_boundary_test.go` |
| 账号状态走环境变量总线 | `account.Gateway` | `account/boundary_test.go` |
| 事件一律可丢 | durable/ephemeral | `eventstream` 测试 |
| 会话元数据四个 sidecar 各写各的 | `internal/session` + 文件锁 | `session/lock_test.go` |
| **巨型 Engine 接口**（A14 点名） | 一个方法的 `TurnEngine` | `engine/boundary_test.go` |
| **能力只声明不执行** | `requireCap` 前置检查 | `control/capability_test.go` |
| **两个注册表两个真源** | `wsEntry` 唯一账本 | `factory_race_test.go` |
| **`.env` 跨工作区串号** | `config.Env` | `config/env_test.go` |

## 6. dsh capability 状态

| 能力 | native | dsh |
|---|---|---|
| streaming | ✅ | ✅ |
| approval | ✅ | ❌ |
| resume | ✅ | ❌ |
| fork | ✅ | ❌ |
| **hosted-tools** | ✅ | ❌ |

**dsh 当前不允许启用。** `boot.selectEngine` 对 `engine = "dsh"` fail-closed，报类型化
`engine.UnsupportedError`，**且有意不留任何配置开关绕过**。原因是 `hosted-tools`：dsh 在
自己的进程里跑自己的工具，等 `tool/call` 推过来时文件已经写完、shell 已经跑过 —— 权限门、
plan mode 只读门、PreToolUse hook、写前检查点、证据链全部落空。事后看一眼不叫门禁。

适配器本身完整（协议、进程守护、事件映射、脱敏、终结状态、环境白名单）。dsh 协议一旦支持
把工具调用委托回 OneCreat 执行，翻一个 capability 就能放行。

## 7. 剩余已知限制

### 阻断"完成"的两项

1. **没有 CI 证据**(§3)。账号级故障 + 本会话无 `actions: write`。**需要用户在
   Settings → Billing → Actions 处理。**
2. **4/14 finding 未闭包**:AR-R03、R05、R06 后半、R08.2/8.3。四条**共用同一个前提** ——
   存在一个跑得起来的 dsh 会话 —— 而 `boot.selectEngine` 对 `engine="dsh"` fail-closed,
   前提不成立。根因是 dsh **`0.1.0-rc.8`** 协议没有 server→client 请求通道(`protocol.go`
   只有三个 client→server 请求与四个**通知**;`linerpc.go:90` 的读循环只认响应与通知,
   `LineClient` 上没有任何 respond/reply)。源码证据与**三条最小决策建议**见总计划 §22,
   **需要用户裁决**。

   原先列在这里的另外两项已经关闭:**R10 后半**(子进程环境按工作区隔离)由用户在 Opus
   任务书 §5 的 C1 中裁决,已落地于 #25/#29;**最终集成复核**已执行,见 §7.5 与总计划 §27。

   另有两条**验收条件**是我自己漏掉、事后补齐的:AR-R09 要求跨进程用例(#23),
   dsh 要求假 sidecar 集成用例而非只编译 adapter(#24)。两条都在补齐时抓到了真问题。

### 其它

- Windows / macOS 只验证到编译,未运行。
- dsh 的取消降级为杀进程(协议无 mid-turn cancel);一个 sidecar 进程就是一个会话
  (协议无 new-session)。
- 内置 UI 的两条行为测试(能力门 #30、对齐缓冲 #31)**没有 node 就跳过**,因此不在 Go
  那条 CI 腿的覆盖里。`internal/serve/index.html` 是内嵌单文件 UI,没有构建步骤,也不在
  `desktop/frontend` 那条 CI 上。
- `codegraph.EnsureInit` 的子进程仍用进程环境(它不读项目 `.env`);codegraph 的 **MCP
  服务器**已按工作区拿到环境(#29)。

### 本会话补做并在过程中查出的三处自身缺陷

如实列出,因为它们说明"守卫强度本身也要被复核":

1. **C1 漏了两处**(#29):`PluginSpecsIn` 只覆盖配置里的插件,而 codegraph 与
   `opts.ExtraPlugins` 是装配后期才追加的 —— `BaseEnv` 为 nil,静默退回进程环境。
   正是我自己在 `boot.go` 注释里写下的失败形态。
2. **B2 的修复里藏着同类 bug**(#31):溢出重试在 `.then` 里同步发起下一次对齐,而前一次的
   `.finally` 排在它之后才跑,把刚建好的缓冲状态清掉。**是把字符串守卫升级成 node 行为
   测试之后当场抓出来的。**
3. **本报告自己有一条会误报的检查**(#32):见 §7.5 的 ⚠️。

前两处都不是"新写的代码有问题",而是"旧守卫太弱,问题一直在那儿没人发现"。

## 7.5 最终集成复核(`main-v2...栈顶`,2026-08-21 02:5x UTC)

复核报告 §7 要求"对 `main-v2...修复分支` 做一次最终集成复核"。**已执行**(不依赖 CI)。
规模:**161 文件,+21571 / -6950**。

| 检查项 | 结果 |
|---|---|
| 删除的文件 | **0** —— 没有任何文件被误删 |
| 非 `.go`/`.md` 改动 | 9 个,全部可解释:`ci.yml`、4 个前端文件、`internal/serve/index.html`、3 份文档 |
| 测试覆盖是否被削弱 | **没有**。全仓库测试函数 1441 → **1623**（+182）。7 个测试文件净减少行数,逐个核对均为**改写**而非删除;`internal/codegraph` 19 → 19（Plan 00 把 Unix-only 用例拆到带 build tag 的新文件） |
| 跨平台假设 | 扫新增文件里的硬编码路径:两处命中。`stopdaemon_unix_test.go` 有 `//go:build !windows` ✅;`dsh/adapter_test.go` 用了 `PATH=/usr/bin` —— **本次改成中性值**,因为那是我无法实跑验证的 Windows 运行期假设 |
| 协议漂移 | `TestKindNamesCoversEveryDeclaredKind` / `TestKindWireNamesAreStable` / `TestEncodeJSONFieldContract` / `TestAppMethodsAreRPCCompatible` / `TestFrontendBindingsAreUpToDate` 全绿 |
| 巨型 facade 回潮 | AST 守卫 `TestAppHoldsNoDomainState` / `internal/control/facade_test.go` 均绿(`App` 与 `Controller` 的**结构体字段**上没有锁,字段白名单也没被突破) |

| goroutine 卫生 | 新增非测试文件里的 `go` 语句逐个核对;`internal/control`、`engine/native`、`engine/dsh` 三个包开着 goleak |
| 资源 double-close | 新增的三处生命周期(`Turn.finish`、`dsh.markDead`、`WorkspaceHandle.Release`)均幂等,有用例 |

> ⚠️ 这条原来写的是「`controller.go` 与 `app.go` 的 `sync.Mutex` 计数均为 0」,是一条**文件级 grep**。
> 它现在会误报:AR-R08.1 给 `eventSink` 加了一把锁,而 `eventSink` 就住在 `app.go` 里 ——
> 那把锁保护它自己的 stamp / seenSession(事件从 agent 运行循环发出,换会话由前端线程触发),
> 与 `App` 是否持有领域状态无关。真正的判据一直是那两条 AST 守卫:它们按
> `type App struct` / `type Controller struct` 的**字段**判定。把粗检查换成守卫名,
> 免得后来人照着跑一遍,看到「1」以为出了回归。


### 复核中发现并处理的一处

**本轮在整个代码库里新增了唯一一处包级可变状态**:`internal/config/env.go` 的
`exported map[string]bool`。规则 6 禁止"新增隐藏全局状态",所以它不能就这么留着。

判断是**可以接受但必须显式**:它镜像的是一个已经存在的全局事实(进程环境 —— `.env` 的
兼容导出写的就是 `os.Setenv`),只增不减、互斥保护,而且它的存在完全依附于那个兼容导出。
已在声明处写明:AR-R10 后半落地(子进程环境显式组装)时,这两个变量应当一并删掉。

### 增量复核(#22–#24,截至 `47104d7`)

上面那次复核做在 #21 的头上,之后又叠了三个分支。对增量重做同一组结构性检查:

| 项 | 结果 |
| --- | --- |
| 规模 | +562 / −8,**产品代码只有 `desktop/app.go` 一处**(AR-R08.1),其余全是测试与文档 |
| 删除的文件 | 0 |
| 新增包级可变状态 | 0(新增的常量都在 `_test.go` 里) |
| 测试函数总数 | 1623 → **1633** |
| 全量 `-race` / 跨平台编译 | 全绿 |

那一处产品改动(事件流身份)在提交时已做过对抗式自审,并有四条用例 + 反向验证。

### 这次复核**没有**覆盖的

- 逐行读完 21571 行新增代码。上面是**结构性**检查(删除、覆盖、平台、协议、状态、资源),
  不等于一次逐行 review。
- Windows / macOS 的**运行期**行为 —— 仍然只有编译。

## 8. 文档一致性

`docs/架构重构执行总计划.md`、`AGENTS.md`、`CLAUDE.md` 已与栈顶代码对齐(AR-R14)。

本轮改写的四处曾经**与代码相反**的陈述:

1. "产品策略对两种引擎一视同仁" —— 假的,dsh 绕过(#15);
2. "事件流可 re-sync" —— 当时前端没有实现(#17);
3. "`runtime.Turn` is still unwired" —— 已接线(#18);
4. workspace 那条只说 `.env` 按 Context 解析,没说它**去哪** —— 补上"`.env` 从不进进程
   环境"与四条显式装配路径(#25)。

状态表不再用一个"已完成"盖住只做了骨架或只有 native 路径的部分;延期项一律加删除线并指向
关闭它的章节,**原文不删**。

### 本报告自己也被复核过

§7.5 里那条 ⚠️:本报告曾经用一条**文件级 grep**("`app.go` 的 mutex 计数为 0")当作"巨型
facade 没有回潮"的判据。它现在会报 1,因为 AR-R08.1 给 `eventSink` 加了一把锁,而
`eventSink` 就住在 `app.go` 里 —— 与 `App` 是否持有领域状态无关。已换成真正的判据(AST
守卫名)。**一个检查的表述方式本身也会过时。**
