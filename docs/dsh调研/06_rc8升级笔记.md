# dsh 0.1.0-rc.7 → 0.1.0-rc.8 升级笔记

> 执行日期 2026-08-20,分支 `chore/dsh-rc8`(从 `feat/dsh-engine` 切出)。
> 上游参考克隆 `/Users/localwork/06_System/_ref/deepseek-harness`,目标 tag `dsh-v0.1.0-rc.8`
> (`git ls-remote --tags origin` 确认这是最新 tag,没有 rc.9 / 正式版,原执行提示词的
> "为什么升 / 风险"两节结论继续有效)。

## Step 0 — 接缝差异复核

逐个 diff 我们**实际依赖**的包(`dsh/profiles/onecreat.cordis.yml` 的那份清单 +
`dsh/plugins/*` 直接 import 的),结论如下。

### 结论一览

| 接缝 | rc.7→rc.8 变化 | 我们要不要改 |
|---|---|---|
| `packages/sdk/protocol`(wire 协议) | **只有 package.json 版本号**,`src/` 零改动 | 不用。方法名/通知名/`JsonRpcLineTransport` 全部原样 |
| `packages/sdk/server`(`HarnessSdkJsonRpcServer`) | `src/index.ts` **+6 行**:`apply()` 里 `initialize` 前加了 `await ctx.get('loader')?.await()`;类的构造签名、`handleRequest`、四种通知**没动** | 构造点不用改。但那 6 行在**官方 `apply()`** 里,我们没加载它 → 我们得**自己补同一条**(见"必须改的清单" #1) |
| `packages/core/agent`、`packages/examples/agent-spine-demo`、`packages/examples/jsonrpc-demo` | **只有版本号**,`src/` 零改动 | 不用。`agent.cancel/inject/followup`、`ctx.agents.get/resume`、profile 里 agent-spine 的 `includeHarnessIdentity/workspaceContext/skills/toolBash/toolJobs` 全部原样;bin 路径仍是 `lib/bin.js` |
| `packages/core/tools` | `src/` 零改动(只动了一个测试) | 不用。`defineTool`、`tools/pre-execute` 钩子(`{kind:'allow'\|'deny'}`)原样 |
| `packages/core/system-prompt` | 只有版本号 | 不用。`includeHarnessIdentity: false` 仍在 |
| `packages/core/session/src/types.ts` | `assistant/message` 事件多了可选的 `interrupted?: true`;`TokenUsage` **未动** | 不用改,但语义有变(见 #4) |
| `packages/core/session/src/known-event-types.ts` | 新增 `team/*` 四个事件类型 | 不用(我们 profile 不装 agent-team) |
| `packages/core/agent-loop/src/agent.ts` | +31 行:turn 被取消时,把已流出的 text/reasoning 前缀补记成一条 `assistant/message`(带 `interrupted: true`),`source: {provider, model}` 用的是**请求里的路由名**(我们这边=`onecreat-gateway`) | 不用改;是改善(取消后的半截回答现在能进 History) |
| `packages/llm/llm/src/retry-policy.ts` | `normal` 模式默认重试 2 → **5**;`always` 模式不再拒绝 `maxRetries`/`retryableCodes` 键 | 不用改(我们没配 retryPolicy);行为变化:失败重试更久,401 之类 AUTH 不在可重试码里,不受影响 |
| `packages/llm/llm/src/assembler.ts`、`content.ts` | 新增 `interruptedBlocks()` / 多模态图片辅助 | 不用 |
| `packages/llm/llm-deepseek`(我们 gateway 插件复用它的 `DeepSeekAdapter`+`resolveAdapterOptions`) | ① **本次升级的主要动机**:`serialize.ts` 的 CoT 回传从"只在带工具调用的轮"改成"**每一个产生了 reasoning 的轮**都回传 `reasoning_content`"(commit 583894f7ae)。② 多模态:`DeepSeekConnectionOptions` **新增必填字段 `maxRequestImageBytes`**,`DeepSeekAdapterOptions` 新增**可选** `resolveAttachments`,catalog model 新增可选 `inputModalities`。③ `httpErrorCode` 把 413 归为 `INVALID_REQUEST` | **不用改代码**:我们的 `options()` 返回的是 `resolveAdapterOptions(...)`,它会填 `maxRequestImageBytes` 默认值(20 MiB);`resolveAttachments` 是可选的,不传即"拒绝图片输入"。但有一条**新的品牌串风险**,见 #3 |
| `packages/plan/plan-mode` | `src/index.ts` 改了 projection 折叠逻辑(`command/done` 参与)+ `/plan` 命令支持图片附件;**`PlanModeController.set(agent, active)` 签名与返回值没动** | 不用改(我们只调 `set`) |
| `packages/interaction/user-approval` | 只有版本号 | 不用。`approval/request` 事件、`ApprovalOutcome` 原样 |
| `packages/compaction/compaction-basic`、`packages/session/session-checkpoint-policy`、`packages/session/session-persistence-jsonl`、`packages/todo/tool-todo`、`packages/mcp/mcp-client`、`packages/llm/token-meter`、`packages/fs/*`(我们用的 `fs-local`/`tool-fs`/`fs-observation-policy`) | 只有版本号 / 无关改动(`tool-fs` 只动了 `read-image.ts`) | 不用 |
| `packages/session/session-persistence-sqlite`("SQLite v2 布局"风险项) | 大改(127 文件) | **与我们无关**:profile 用的是 `@deepseek-ai/dsh-session-persistence-jsonl`,不是 sqlite。原风险清单里"旧会话不可 load"的主因被排除 |
| `packages/session/session-persistence/src/coordinator.ts` | 3 行:`seed` 不再 `structuredClone` | 不用 |
| `packages/shell/tool-pwsh-persistent`(新包)、`subprocess-local` 的 Windows inspector | 新增/增强,Windows 侧 | 不用(我们 profile 只装 `bash-local`);Windows 上 `subprocess-local` 的进程树处理变好是白拿的 |
| 包名 | `@deepseek-ai/dsh-sdk-jsonrpc-server` / `dsh-sdk-protocol` / `dsh-sdk-jsonrpc-demo` **全部未改名**,rc.8 均已发到 npm(22 个包逐个 `npm view` 核过) | 不用 |
| `@deepseek-ai/cordis` | rc.8 的 peer 仍是 `^4.0.1` | 保持 `4.0.1` |

### 必须改的清单

1. **`dsh/plugins/control/index.js` 补 loader await**(对应上游 sdk/server 的那 6 行)。
   我们的控制面自己拥有 transport、自己分派 `initialize`,拿不到官方 `apply()` 里的修复。
   而我们**恰恰**在同一个 `apply()` 里 `ctx.plugin(McpClient, …)` 挂硬件 MCP —— 这正是上游注释
   点名的场景(MCP 首轮工具发现是异步 Loader 条目)。不补,`initialize` 可能在硬件工具注册完成前
   就应答,首轮出现"工具还没挂上"的窗口期。
2. **版本号锁到 rc.8**:`dsh/package.json` 22 个 `@deepseek-ai/dsh-*` 依赖 + 重新生成 `pnpm-lock.yaml`。
3. **新的品牌串风险(rc.8 引入)**:`llm-deepseek/src/adapter.ts` 新增两条面向用户的错误文案,
   都以 `DeepSeek` 开头 —— `DeepSeek model "…" does not accept image input.` 与
   `DeepSeek image conversion requires the durable attachment service.`。
   只在"请求内容里含 image 块"时才触发,OneCreat 当前不往 dsh 送图片(Go 侧只发 text 块),
   所以触不到。核过 `internal/boot/dshengine.go` 的 `dshBrandSecrets`:名单里已有
   `"DeepSeek"` 与 `"deepseek"` 两条通配,**这两句已被现成的兜底盖住,不需要加名单**;
   补一条回归测试 `TestScrubberCoversRC8ImageErrors` 把这个事实钉住,免得日后有人精简名单。
4. **语义变化,无需改码但要知道**:取消一轮后现在会多出一条 `assistant/message`
   (`interrupted: true`)。`mapper.Map` 会把它映射成一条 `event.Message`,内容是已流出的前缀 ——
   与正常轮的"chunk 流式 + message 收尾"是同一形状,前端无需改。
5. 文档/注释里的 `0.1.0-rc.7` 字样(`CLAUDE.md`、`dsh/README.md`、`internal/config/render.go` 的
   注释样例 + 对应 round-trip 测试、`internal/engine/dsh/protocol.go` 包注释、两个测试里的固定串)。

### 与原执行提示词预判不符的地方(以实际 diff 为准)

- 预判"Session 存储改了 SQLite v2 布局 → resume 与落盘扫描必须复验"——**不适用**:
  我们用 JSONL 持久化,`session-persistence-jsonl` 本次零源码改动。旧会话兼容性风险大幅下降
  (仍按要求实测,见下方回归表)。
- 预判"`HarnessSdkJsonRpcServer` 构造签名/transport 行为可能变"——**没变**,这个最脆的接缝这次是安全的。
- 预判"`dsh-sdk-jsonrpc-demo` 与 agent-teams 有改名动作"——**没改名**,bin 仍是 `lib/bin.js`。
- 预判"品牌 branding 动了不少 → `includeHarnessIdentity` 可能没了"——**仍在**,`system-prompt` 包零源码改动。
- 新发现(原提示词没提):`llm` 的默认重试次数 2→5,以及 rc.8 新增的两条含 `DeepSeek` 字样的
  图片错误文案(见 #3)。

---

## 回归结果

见文末 §"Step 2/3 回归结果表"。
